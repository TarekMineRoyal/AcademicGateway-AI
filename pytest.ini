[pytest]
filterwarnings =
    ignore::starlette.StarletteDeprecationWarning
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    unit: Fast, isolated unit tests.
    integration: Database and repository tests.
    smoke: External framework and ML prefix tests.
    api: API boundary and error handling tests.